<h1>All Pets</h1>

<% @pets.each do |pet| -%>
  <h3><%= pet.name %></h3>
  <%= img_tag "#{pet.image}" %>
  <%= "Sex: #{pet.sex}" %>
  <%= "Age: #{pet.age}" %>
  <%= "Shelter: #{Shelter.find(pet.shelter_id).name}" %>
<% end %>
